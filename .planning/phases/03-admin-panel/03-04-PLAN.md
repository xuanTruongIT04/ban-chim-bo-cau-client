---
phase: 03-admin-panel
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified: []
autonomous: false
requirements:
  - APROD-01
  - APROD-02
  - APROD-03
  - APROD-04
  - APROD-05
  - AORD-01
  - AORD-02
  - AORD-03
  - DASH-01
  - DASH-03

must_haves:
  truths:
    - "Admin can log in and see the dashboard with real data"
    - "Admin can create, edit, and delete products"
    - "Admin can upload product images"
    - "Admin can view, filter, and manage orders through the status workflow"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete admin panel — dashboard, product management, and order management. This is the final checkpoint before Phase 3 is marked complete.

Purpose: Confirm that all admin surfaces work correctly with the live backend before proceeding to Phase 4.
Output: Verified admin panel or list of issues to fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-admin-panel/03-02-SUMMARY.md
@.planning/phases/03-admin-panel/03-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify complete admin panel</name>
  <files>src/pages/admin/DashboardPage.tsx, src/pages/admin/ProductsPage.tsx, src/pages/admin/OrdersPage.tsx</files>
  <read_first>
    src/router/index.tsx,
    src/pages/admin/DashboardPage.tsx,
    src/pages/admin/ProductsPage.tsx,
    src/pages/admin/OrdersPage.tsx
  </read_first>
  <action>
    Human verification checkpoint. Present the verification steps below to the user and wait for approval.

    Prerequisites: Backend Laravel running on localhost:8000. Admin account exists.

    **1. Dashboard (DASH-01, DASH-03):**
    - Navigate to http://localhost:5173/admin/login, log in as admin
    - Verify dashboard shows stat cards with order counts by status
    - Verify "Don hang moi" card shows count
    - Verify product stock card shows in-stock count ("Dang ban: N") and out-of-stock count ("Het hang: N")

    **2. Product Management (APROD-01 through APROD-05):**
    - Navigate to /admin/products
    - Verify product table loads with pagination
    - Click "Them san pham" — fill form, submit — verify new product appears in table
    - Click Edit on a product — verify form pre-fills — change a field, submit — verify change reflects
    - Click Upload image on a product — upload a JPEG — verify image appears
    - Click Delete on a product — verify confirmation dialog — confirm — verify product removed from table
    - Verify stock adjustment: click stock adjust, enter delta, submit

    **3. Order Management (AORD-01 through AORD-03):**
    - Navigate to /admin/orders
    - Verify order table loads with pagination
    - Use status dropdown to filter — verify table updates
    - Use search to find by customer name — verify results
    - Click "Chi tiet" on an order — verify drawer opens with customer info and items
    - If order is "Cho xac nhan": click "Xac nhan don" — verify status changes
    - If order is "Xac nhan": click "Bat dau giao" — verify status changes
    - Try "Huy don" on a non-terminal order — verify confirmation dialog — confirm — verify status changes to Huy
    - Verify terminal orders (Hoan thanh, Huy) show no action buttons
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard loads without errors and shows stat cards
    - Dashboard shows in-stock vs out-of-stock product split (DASH-03)
    - Product CRUD works end-to-end
    - Image upload works (image appears after upload)
    - Order filtering works for all 3 filter types
    - Order status transitions follow the state machine correctly
    - Cancel uses confirmation dialog
  </acceptance_criteria>
  <done>User has confirmed all 10 Phase 3 requirements work correctly with the live backend. Admin panel is usable end-to-end.</done>
</task>

</tasks>

<verification>
Human visual and functional verification of the complete admin panel against all Phase 3 requirements.
</verification>

<success_criteria>
- All 10 Phase 3 requirements (APROD-01-05, AORD-01-03, DASH-01, DASH-03) verified working
- No console errors during normal admin workflows
- Admin panel is usable end-to-end with real backend data
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-panel/03-04-SUMMARY.md`
</output>
