---
phase: 01-foundation-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - vite.config.ts
  - vitest.config.ts
  - eslint.config.js
  - .prettierrc
  - .prettierignore
  - .env.development
  - .env.staging
  - .env.production
  - .env.example
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/vite-env.d.ts
  - src/config/env.ts
  - src/types/api.ts
  - src/api/.gitkeep
  - src/stores/.gitkeep
  - src/hooks/.gitkeep
  - src/components/.gitkeep
  - src/layouts/.gitkeep
  - src/pages/.gitkeep
  - src/router/.gitkeep
  - src/lib/.gitkeep
  - src/test/setup.ts
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03

must_haves:
  truths:
    - "Running `npm run dev` starts a Vite dev server without errors"
    - "Running `npm run build` produces a production build without errors"
    - "Running `npm run test -- --run` executes vitest without errors"
    - "Environment variable VITE_API_BASE_URL is accessible in application code"
    - "Project has clean architecture folder structure under src/"
  artifacts:
    - path: "package.json"
      provides: "All production and dev dependencies installed"
      contains: "react"
    - path: "vite.config.ts"
      provides: "Vite 6 configuration with React plugin"
      contains: "@vitejs/plugin-react"
    - path: "src/config/env.ts"
      provides: "Typed environment variable wrapper"
      contains: "VITE_API_BASE_URL"
    - path: "src/types/api.ts"
      provides: "Shared TypeScript types for API contracts"
      contains: "UserProfile"
    - path: "vitest.config.ts"
      provides: "Test configuration with jsdom"
      contains: "jsdom"
  key_links:
    - from: "src/main.tsx"
      to: "src/App.tsx"
      via: "ReactDOM.createRoot import"
      pattern: "import.*App"
---

<objective>
Scaffold the Vite 6 + React 18 + TypeScript project from scratch with all production and dev dependencies, clean architecture folder structure, multi-environment configuration, shared TypeScript types, and test infrastructure.

Purpose: Establish the project foundation so all subsequent plans can import from established paths and run dev/build/test commands immediately.
Output: A working Vite project with all dependencies installed, folder structure created, env files configured, and vitest running.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-authentication/01-CONTEXT.md
@.planning/phases/01-foundation-authentication/01-RESEARCH.md
@.planning/phases/01-foundation-authentication/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite project, install all dependencies, create folder structure</name>
  <files>
    package.json, tsconfig.json, tsconfig.app.json, tsconfig.node.json, vite.config.ts,
    index.html, src/main.tsx, src/App.tsx, src/vite-env.d.ts,
    eslint.config.js, .prettierrc, .prettierignore,
    src/api/.gitkeep, src/stores/.gitkeep, src/hooks/.gitkeep,
    src/components/.gitkeep, src/components/common/.gitkeep,
    src/layouts/.gitkeep, src/pages/.gitkeep, src/pages/admin/.gitkeep,
    src/pages/customer/.gitkeep, src/router/.gitkeep, src/lib/.gitkeep,
    src/config/.gitkeep, src/types/.gitkeep, src/test/.gitkeep
  </files>
  <read_first>
    .planning/phases/01-foundation-authentication/01-RESEARCH.md (installation commands and version pins)
    .planning/phases/01-foundation-authentication/01-CONTEXT.md (locked decisions)
    CLAUDE.md (project conventions and stack)
  </read_first>
  <action>
    1. Scaffold Vite project in the current directory (project root is already the repo root):
       ```bash
       npm create vite@6 . -- --template react-ts
       ```
       If the directory is non-empty (has .planning/, .git), scaffold into a temp dir and move files:
       ```bash
       npm create vite@6 temp-scaffold -- --template react-ts
       # Move generated files (package.json, vite.config.ts, tsconfig*, index.html, src/) into project root
       # Then remove temp-scaffold
       ```

    2. Remove default Vite scaffold content from src/ (App.css, index.css, assets/, etc.) but keep src/vite-env.d.ts.

    3. Install pinned production dependencies (per D-01 through D-31 and RESEARCH.md version table):
       ```bash
       npm install react@^18.3 react-dom@^18.3 react-router-dom@^6.26 zustand@^5.0 axios@^1.7 @tanstack/react-query@^5.51 antd@^5.20 @ant-design/icons@^5.4 zod@^3.23
       ```

    4. Install pinned dev dependencies:
       ```bash
       npm install -D typescript@^5.5 vitest@^2.1 @testing-library/react@^16.0 @testing-library/user-event@^14.0 @testing-library/jest-dom@^6.0 jsdom@^25.0 eslint@^9.0 typescript-eslint@^8.0 prettier@^3.3 eslint-config-prettier eslint-plugin-react-hooks @types/react @types/react-dom
       ```

    5. Create clean architecture folder structure (per FOUND-02, RESEARCH.md architecture):
       ```
       src/
       ├── api/           (Axios instance, API modules)
       ├── stores/        (Zustand stores)
       ├── hooks/         (Custom React hooks)
       ├── components/
       │   └── common/    (Shared components: AdminRoute, PageLoader, etc.)
       ├── layouts/       (AdminLayout, CustomerLayout)
       ├── pages/
       │   ├── admin/     (Admin pages)
       │   └── customer/  (Customer pages)
       ├── router/        (Route configuration)
       ├── lib/           (Utility modules: navigationService)
       ├── config/        (Environment config, AntD theme)
       ├── types/         (Shared TypeScript types)
       └── test/          (Test setup)
       ```
       Use .gitkeep files in empty directories to preserve structure in git.

    6. Create ESLint flat config file `eslint.config.js`:
       ```js
       import js from '@eslint/js';
       import tseslint from 'typescript-eslint';
       import reactHooks from 'eslint-plugin-react-hooks';
       import prettierConfig from 'eslint-config-prettier';

       export default [
         js.configs.recommended,
         ...tseslint.configs.recommended,
         { plugins: { 'react-hooks': reactHooks }, rules: reactHooks.configs.recommended.rules },
         prettierConfig,
         { ignores: ['dist/', 'node_modules/'] },
       ];
       ```

    7. Create `.prettierrc`:
       ```json
       {
         "semi": true,
         "singleQuote": true,
         "trailingComma": "all",
         "printWidth": 100,
         "tabWidth": 2
       }
       ```

    8. Create `.prettierignore`:
       ```
       dist/
       node_modules/
       ```

    9. Create minimal `src/main.tsx`:
       ```tsx
       import React from 'react';
       import ReactDOM from 'react-dom/client';
       import App from './App';

       ReactDOM.createRoot(document.getElementById('root')!).render(
         <React.StrictMode>
           <App />
         </React.StrictMode>,
       );
       ```

    10. Create minimal `src/App.tsx` (placeholder, will be expanded in Plan 03):
        ```tsx
        export default function App() {
          return <div>Ban Chim Bo Cau - Loading...</div>;
        }
        ```

    11. Add scripts to package.json if not already present:
        ```json
        "scripts": {
          "dev": "vite",
          "build": "tsc -b && vite build",
          "preview": "vite preview",
          "lint": "eslint .",
          "format": "prettier --write .",
          "test": "vitest"
        }
        ```

    12. Ensure `tsconfig.json` has `"strict": true` and path aliases are NOT configured (keep simple imports for this project size).
  </action>
  <verify>
    <automated>cd D:/BANCHIMBOCAU/ban-chim-bo-cau-client && npm run build</automated>
  </verify>
  <done>
    - package.json contains "react": "^18.3"
    - package.json contains "zustand": "^5.0"
    - package.json contains "antd": "^5.20"
    - package.json contains "vitest" in devDependencies
    - tsconfig.json or tsconfig.app.json contains "strict": true
    - vite.config.ts contains "@vitejs/plugin-react"
    - src/api/ directory exists
    - src/stores/ directory exists
    - src/hooks/ directory exists
    - src/components/common/ directory exists
    - src/layouts/ directory exists
    - src/pages/admin/ directory exists
    - src/pages/customer/ directory exists
    - src/router/ directory exists
    - src/lib/ directory exists
    - src/config/ directory exists
    - src/types/ directory exists
    - `npm run build` exits with code 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Create environment config, shared types, and test infrastructure</name>
  <files>
    .env.development, .env.staging, .env.production, .env.example,
    src/config/env.ts, src/types/api.ts, vitest.config.ts, src/test/setup.ts
  </files>
  <read_first>
    package.json (verify dependencies are installed)
    .planning/phases/01-foundation-authentication/01-RESEARCH.md (Pattern 6: env config, types)
    .planning/phases/01-foundation-authentication/01-CONTEXT.md (D-02, D-04, D-05 for API types)
    .planning/phases/01-foundation-authentication/01-VALIDATION.md (Wave 0 requirements)
  </read_first>
  <action>
    1. Create `.env.development`:
       ```
       VITE_API_BASE_URL=http://localhost:8000/api
       ```

    2. Create `.env.staging`:
       ```
       VITE_API_BASE_URL=https://staging.banchimbocau.vn/api
       ```

    3. Create `.env.production`:
       ```
       VITE_API_BASE_URL=https://api.banchimbocau.vn/api
       ```

    4. Create `.env.example`:
       ```
       VITE_API_BASE_URL=http://localhost:8000/api
       ```

    5. Create `src/config/env.ts` — typed wrapper (per RESEARCH.md Pattern 6):
       ```typescript
       export const env = {
         apiBaseUrl: import.meta.env.VITE_API_BASE_URL as string,
         isDev: import.meta.env.DEV as boolean,
         isProd: import.meta.env.PROD as boolean,
       } as const;
       ```

    6. Create `src/types/api.ts` — shared TypeScript types derived from decisions D-02, D-04, D-05:
       ```typescript
       // Auth API types — per D-02
       export interface LoginRequest {
         email: string;
         password: string;
       }

       // Per D-02: POST /api/auth/login response
       export interface LoginResponse {
         access_token: string;
         token_type: 'Bearer';
       }

       // Per D-04, D-05: GET /api/me response
       export interface UserProfile {
         id: number;
         name: string;
         email: string;
         role: 'admin' | 'customer';
       }

       // Generic API error shape
       export interface ApiError {
         message: string;
         errors?: Record<string, string[]>;
       }
       ```

    7. Create `vitest.config.ts`:
       ```typescript
       import { defineConfig } from 'vitest/config';
       import react from '@vitejs/plugin-react';

       export default defineConfig({
         plugins: [react()],
         test: {
           environment: 'jsdom',
           globals: true,
           setupFiles: ['./src/test/setup.ts'],
         },
       });
       ```

    8. Create `src/test/setup.ts`:
       ```typescript
       import '@testing-library/jest-dom';
       ```

    9. Add to `.gitignore` (if not already present):
       ```
       .env.local
       .env.*.local
       ```
       Do NOT gitignore .env.development, .env.staging, .env.production as they contain no secrets.
  </action>
  <verify>
    <automated>cd D:/BANCHIMBOCAU/ban-chim-bo-cau-client && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>
    - .env.development contains VITE_API_BASE_URL=http://localhost:8000/api
    - .env.staging contains VITE_API_BASE_URL=https://staging.banchimbocau.vn/api
    - .env.production contains VITE_API_BASE_URL=https://api.banchimbocau.vn/api
    - src/config/env.ts contains "apiBaseUrl"
    - src/config/env.ts contains "VITE_API_BASE_URL"
    - src/types/api.ts contains "interface LoginRequest"
    - src/types/api.ts contains "interface LoginResponse"
    - src/types/api.ts contains "interface UserProfile"
    - src/types/api.ts contains "role: 'admin' | 'customer'"
    - src/types/api.ts contains "access_token: string"
    - vitest.config.ts contains "environment: 'jsdom'"
    - vitest.config.ts contains "setupFiles"
    - src/test/setup.ts contains "@testing-library/jest-dom"
    - `npx vitest run` exits with code 0 (no tests yet, but config is valid)
  </done>
</task>

</tasks>

<verification>
- `npm run dev` starts Vite dev server on localhost
- `npm run build` produces dist/ folder without errors
- `npx vitest run` completes without configuration errors
- All src/ subdirectories exist per FOUND-02
- .env.development has correct VITE_API_BASE_URL
</verification>

<success_criteria>
- Build succeeds with zero errors
- Vitest configuration is valid
- All 3 environment files exist with VITE_API_BASE_URL
- TypeScript types match the Laravel auth contract (D-02, D-04, D-05)
- Clean architecture folder structure matches FOUND-02 specification
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-authentication/01-01-SUMMARY.md`
</output>
