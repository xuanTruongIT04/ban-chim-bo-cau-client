---
phase: 02-customer-purchase-flow
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/pages/customer/CartPage.tsx
  - src/components/customer/CartItemRow.tsx
  - src/layouts/CustomerLayout.tsx
autonomous: true
requirements:
  - CART-01
  - CART-02
  - CART-03
  - CART-04
  - CART-05

must_haves:
  truths:
    - "Visitor can view cart with item list, quantities, and total"
    - "Visitor can update item quantity with debounced PATCH"
    - "Visitor can remove item with immediate DELETE"
    - "Cart total updates in real-time from backend data"
    - "Cart badge in header shows correct item count from backend"
    - "Empty cart shows 'Gio hang trong' with continue shopping link"
    - "Cart persists across page refresh via cart token in localStorage"
  artifacts:
    - path: "src/pages/customer/CartPage.tsx"
      provides: "Cart page with item list and order summary"
      min_lines: 80
    - path: "src/components/customer/CartItemRow.tsx"
      provides: "Individual cart item with quantity control and remove"
      min_lines: 30
    - path: "src/layouts/CustomerLayout.tsx"
      provides: "Updated header with cart badge from backend data"
      contains: "useCart"
  key_links:
    - from: "src/pages/customer/CartPage.tsx"
      to: "src/hooks/useCart.ts"
      via: "useCart, useUpdateCartItem, useRemoveCartItem hooks"
      pattern: "useCart|useUpdateCartItem|useRemoveCartItem"
    - from: "src/layouts/CustomerLayout.tsx"
      to: "src/hooks/useCart.ts"
      via: "useCart for badge count"
      pattern: "useCart"
---

<objective>
Build the cart page and update the header cart badge to use backend cart data instead of the old local-only store.

Purpose: Visitors can manage their cart (view, update quantities, remove items) and see accurate cart state in the header.
Output: Cart page, cart item row component, updated CustomerLayout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-customer-purchase-flow/02-RESEARCH.md
@.planning/phases/02-customer-purchase-flow/02-UI-SPEC.md
@.planning/phases/02-customer-purchase-flow/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->

From src/types/api.ts:
```typescript
export interface CartItemResource {
  id: number;            // cart item ID for PATCH/DELETE
  product_id: number;
  product_name: string;
  product_price_vnd: number;
  quantity: string;      // decimal string "2.000"
  subtotal: number;      // VND integer
  is_available: boolean;
}
export interface CartData {
  id: number; token: string; expires_at: string;
  items: CartItemResource[]; total_amount: number;
}
```

From src/hooks/useCart.ts:
```typescript
export function useCart(): UseQueryResult<CartData | null>;
export function useUpdateCartItem(): UseMutationResult;
export function useRemoveCartItem(): UseMutationResult;
```

From src/utils/format.ts:
```typescript
export function formatVND(amount: number): string;
```

From src/stores/cartStore.ts (refactored in Plan 01):
```typescript
export const useCartStore = create<CartState>()(persist(...));
// cartToken: string | null; setCartToken; clearCartToken
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CartItemRow component and CartPage</name>
  <files>
    src/components/customer/CartItemRow.tsx,
    src/pages/customer/CartPage.tsx
  </files>
  <read_first>
    src/types/api.ts,
    src/hooks/useCart.ts,
    src/utils/format.ts,
    .planning/phases/02-customer-purchase-flow/02-UI-SPEC.md
  </read_first>
  <action>
    **Step 1: Create src/components/customer/CartItemRow.tsx**

    Per UI-SPEC.md Cart Page spec. Uses AntD `List.Item`, `InputNumber`, `Button`, `Typography`.

    Props: `{ item: CartItemResource; onUpdateQuantity: (itemId: number, quantity: number) => void; onRemove: (itemId: number) => void; updating: boolean }`

    Layout within List.Item:
    - Left: product name (`Typography.Text` strong), price per unit (`Typography.Text` type="secondary" — `formatVND(item.product_price_vnd)` + ` / con`)
    - Center: `InputNumber` with `min={1}`, `step={1}`, `precision={0}`, `value={parseInt(item.quantity)}` (parse decimal string to integer), `onChange` calls debounced `onUpdateQuantity(item.id, newQty)` with 500ms debounce using a local `useRef` + `setTimeout` pattern
    - Right: subtotal (`Typography.Text` strong — `formatVND(item.subtotal)`), Remove button (`Button` type="text" danger with `DeleteOutlined` icon, onClick calls `onRemove(item.id)`)
    - "Xóa" text label on remove button per UI-SPEC.md — no confirmation modal needed (low-stakes action)

    Debounce implementation: use `useRef<ReturnType<typeof setTimeout>>()` to track timeout. On InputNumber change, clear previous timeout and set new 500ms timeout that calls `onUpdateQuantity`. Clean up on unmount with `useEffect` return.

    IMPORTANT: `item.quantity` is a decimal string like "2.000" — always use `parseInt(item.quantity)` for display and InputNumber value.

    **Step 2: Create src/pages/customer/CartPage.tsx**

    Per UI-SPEC.md Cart Page spec.

    Data: `useCart()` for cart data, `useUpdateCartItem()` for quantity updates, `useRemoveCartItem()` for removals.

    Layout:
    - Page max-width 1200px, centered, padding 24px
    - Title: `Typography.Title` level={2} "Giỏ hàng"
    - Two columns on desktop (`Row` + `Col`): items column (`Col` md={16}), summary column (`Col` md={8}). Stacked on mobile.

    Items column:
    - `List` component with `dataSource={cart.items}`, `renderItem` returns `CartItemRow`
    - Pass `onUpdateQuantity` that calls `updateCartItem.mutate({ itemId, quantity })`
    - Pass `onRemove` that calls `removeCartItem.mutate(itemId)`

    Summary column:
    - AntD `Card` with title "Tóm tắt đơn hàng"
    - Item count: `Typography.Text` — `${cart.items.length} sản phẩm`
    - Total: `Typography.Title` level={3} with colorPrimary style — `formatVND(cart.total_amount)` (use backend-computed total, NOT frontend sum)
    - "Tiến hành đặt hàng" `Button` type="primary" block, `onClick` navigates to `/checkout` via `useNavigate()`, disabled if `!cart || cart.items.length === 0`

    Empty state (no cart token or cart with 0 items):
    - AntD `Empty` with description "Giỏ hàng trống" and body "Bạn chưa có sản phẩm nào trong giỏ hàng."
    - "Tiếp tục mua sắm" `Button` type="primary" linking to `/`

    Loading state: AntD `Spin` centered while `useCart` is loading.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/components/customer/CartItemRow.tsx contains `parseInt(item.quantity)`
    - src/components/customer/CartItemRow.tsx contains `DeleteOutlined`
    - src/components/customer/CartItemRow.tsx contains `setTimeout` (debounce)
    - src/components/customer/CartItemRow.tsx contains `500` (debounce ms)
    - src/pages/customer/CartPage.tsx contains `useCart`
    - src/pages/customer/CartPage.tsx contains `useUpdateCartItem`
    - src/pages/customer/CartPage.tsx contains `useRemoveCartItem`
    - src/pages/customer/CartPage.tsx contains `Tóm tắt đơn hàng`
    - src/pages/customer/CartPage.tsx contains `Tiến hành đặt hàng`
    - src/pages/customer/CartPage.tsx contains `Giỏ hàng trống`
    - src/pages/customer/CartPage.tsx contains `Tiếp tục mua sắm`
    - src/pages/customer/CartPage.tsx contains `formatVND(cart`  (uses backend total)
    - src/pages/customer/CartPage.tsx does NOT contain `price * quantity` or manual total calculation
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>CartPage shows items with quantity controls (debounced PATCH), remove buttons, order summary with backend-computed total, and empty/loading states. CartItemRow handles debounced quantity updates and decimal string parsing.</done>
</task>

<task type="auto">
  <name>Task 2: Update CustomerLayout cart badge to use backend cart data</name>
  <files>
    src/layouts/CustomerLayout.tsx
  </files>
  <read_first>
    src/layouts/CustomerLayout.tsx,
    src/hooks/useCart.ts,
    src/stores/cartStore.ts
  </read_first>
  <action>
    Update `src/layouts/CustomerLayout.tsx` to replace the old `useCartStore((s) => s.items.length)` with backend cart data.

    Changes:
    1. Remove import: `import { useCartStore } from '../stores/cartStore';`
    2. Add import: `import { useCart } from '../hooks/useCart';`
    3. Replace `const cartCount = useCartStore((s) => s.items.length);` with:
       ```typescript
       const { data: cart } = useCart();
       const cartCount = cart?.items?.length ?? 0;
       ```
    4. The rest of CustomerLayout stays exactly the same — Badge, Header, Menu, Link, Outlet, etc.

    This ensures the cart badge count reflects actual backend cart state, and updates when TanStack Query invalidates the `['cart']` query after add/update/remove mutations.

    Note: `useCart()` is `enabled: !!cartToken` — when no cart token exists (fresh visitor), it returns `null` and cartCount is 0. This is correct behavior.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/layouts/CustomerLayout.tsx contains `import { useCart } from`
    - src/layouts/CustomerLayout.tsx contains `useCart()`
    - src/layouts/CustomerLayout.tsx contains `cart?.items?.length`
    - src/layouts/CustomerLayout.tsx does NOT contain `useCartStore` import
    - src/layouts/CustomerLayout.tsx does NOT contain `s.items.length`
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>CustomerLayout cart badge reads item count from backend cart data via TanStack Query instead of old local-only Zustand store.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — no TypeScript errors
- `npx vitest run` — all tests still pass
- Manual: navigate to `/cart` — empty state shown for fresh visitor
- Manual: add item from product page, navigate to `/cart` — item shown with quantity and total
- Manual: header badge updates when items are added
</verification>

<success_criteria>
- Cart page shows items with quantity controls and backend-computed total
- Quantity updates are debounced at 500ms before sending PATCH
- Remove button immediately fires DELETE
- Empty cart state shows with "Tiếp tục mua sắm" link
- Header cart badge shows correct count from backend
- Cart data persists via cart_token in localStorage — refresh preserves cart
</success_criteria>

<output>
After completion, create `.planning/phases/02-customer-purchase-flow/02-03-SUMMARY.md`
</output>
