---
phase: 04-account-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/customer/HomePage.tsx
  - src/pages/customer/CartPage.tsx
  - src/pages/customer/CheckoutPage.tsx
  - src/pages/customer/ProductDetailPage.tsx
  - src/pages/admin/ProductsPage.tsx
  - src/pages/admin/OrdersPage.tsx
  - src/pages/admin/DashboardPage.tsx
  - src/components/admin/ProductFormModal.tsx
  - src/components/admin/StockAdjustModal.tsx
  - src/components/admin/OrderDetailDrawer.tsx
  - src/components/customer/CategoryFilter.tsx
  - src/api/axiosInstance.ts
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-03
must_haves:
  truths:
    - "Every data-fetching page shows a loading indicator while API call is in flight"
    - "Every API error produces a visible notification or inline error — no silent failures"
    - "Every form shows inline validation errors before submission"
    - "TanStack Query error states are surfaced to users on all pages"
  artifacts:
    - path: "src/pages/customer/CheckoutPage.tsx"
      provides: "Checkout with loading spinner during cart fetch"
      contains: "Spin"
    - path: "src/components/admin/OrderDetailDrawer.tsx"
      provides: "Order drawer with error and loading states"
      contains: "isError"
    - path: "src/api/axiosInstance.ts"
      provides: "Global error interceptor covering all status codes"
      contains: "message.error"
  key_links:
    - from: "All pages with useQuery"
      to: "User-visible feedback"
      via: "isLoading -> Spin/skeleton, isError -> Alert/Result"
      pattern: "isLoading|isError"
---

<objective>
Audit and fill all UX gaps: loading states, error handling, and form validation across every page.

Purpose: UX-01 requires every data-fetching surface to show loading indicators. UX-02 requires every API error to produce visible notification. UX-03 requires every form to show inline validation. Many pages already have these, but a systematic audit will find and fix gaps.

Output: All pages updated with consistent loading/error/validation patterns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/api/axiosInstance.ts

<interfaces>
<!-- Current UX state audit from codebase review -->

LOADING STATES (UX-01) — Current status:
- HomePage: ProductGrid has `loading` prop (GOOD) but no skeleton
- ProductDetailPage: Spin while loading (GOOD)
- CartPage: Spin while loading (GOOD)
- CheckoutPage: Returns null during cart load (GAP — no spinner, just blank)
- DashboardPage: Spin while loading (GOOD)
- ProductsPage: Table has `loading` prop (GOOD)
- OrdersPage: Table has `loading` prop (GOOD)
- CategoryFilter: No loading state visible (MINOR GAP)

ERROR STATES (UX-02) — Current status:
- HomePage: Result with retry button on error (GOOD)
- ProductDetailPage: Result 404 on error (GOOD)
- CartPage: No error handling for useCart error (GAP)
- CheckoutPage: 422 errors handled, generic error via message (GOOD)
- DashboardPage: Alert on error (GOOD)
- ProductsPage: No error handling (GAP — only loading, no isError)
- OrdersPage: No error handling (GAP — only loading, no isError)
- OrderDetailDrawer: Need to check (POSSIBLE GAP)
- axiosInstance: 401 and 5xx handled globally (GOOD), 422 pass-through (GOOD)
- TanStack Query mutations: Most have onError with message.error (GOOD)

FORM VALIDATION (UX-03) — Current status:
- LoginPage: Need to check (POSSIBLE GAP)
- CheckoutPage: AntD rules with required + pattern (GOOD)
- ProductFormModal: AntD rules with required + min (GOOD)
- StockAdjustModal: AntD rules with required + type (GOOD)
- OrdersPage filters: No validation needed (N/A — optional filters)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix loading states + error handling on all customer pages</name>
  <files>
    src/pages/customer/HomePage.tsx,
    src/pages/customer/CartPage.tsx,
    src/pages/customer/CheckoutPage.tsx,
    src/pages/customer/ProductDetailPage.tsx,
    src/components/customer/CategoryFilter.tsx
  </files>
  <read_first>
    src/pages/customer/HomePage.tsx,
    src/pages/customer/CartPage.tsx,
    src/pages/customer/CheckoutPage.tsx,
    src/pages/customer/ProductDetailPage.tsx,
    src/components/customer/CategoryFilter.tsx,
    src/hooks/useCart.ts,
    src/hooks/useProducts.ts,
    src/hooks/useCategories.ts
  </read_first>
  <action>
  **Fixes to apply (per UX-01, UX-02):**

  **CheckoutPage.tsx (GAP — blank screen during cart load):**
  - Replace `if (isLoading || !cart) { return null; }` with a centered `<Spin size="large" />` wrapped in a flex container (same pattern as CartPage)
  - Add error handling: if useCart returns isError, show `<Result status="error">` with retry button

  **CartPage.tsx (GAP — no error state):**
  - Add `isError` and `error` destructuring from `useCart()`
  - After the loading check, add: `if (isError) { return <Result status="error" title="Khong tai duoc gio hang" extra={<Button onClick={() => refetch()}>Thu lai</Button>} /> }`
  - Destructure `refetch` from useCart as well

  **HomePage.tsx (MINOR — enhance loading):**
  - Already has loading via ProductGrid `loading` prop — verify this shows a spinner not just blank grid
  - If ProductGrid does not show a loading indicator when `loading=true`, add `<Spin>` wrapper

  **CategoryFilter.tsx (MINOR GAP):**
  - Check if useCategories exposes isLoading — if so, pass `loading` prop to the Select component
  - AntD Select supports a `loading` prop natively

  **ProductDetailPage.tsx:**
  - Already good — Spin on loading, Result on error. No changes needed.

  **General pattern for all fixes:**
  - Loading: Use `<Spin size="large" />` centered in a flex container with minHeight 300-400px
  - Error: Use `<Result status="error" title="..." subTitle="..." extra={<Button onClick={refetch}>Thu lai</Button>} />`
  - Import `message` from antd where needed for mutation error toasts
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
  - CheckoutPage shows spinner while cart loads (not blank screen)
  - CartPage shows error state with retry on useCart failure
  - CategoryFilter shows loading indicator while categories fetch
  - All customer pages have both loading AND error states for every useQuery call
  </acceptance_criteria>
  <done>Every customer-facing data-fetching surface has visible loading indicator and error state</done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix loading/error states on all admin pages + form validation gaps</name>
  <files>
    src/pages/admin/ProductsPage.tsx,
    src/pages/admin/OrdersPage.tsx,
    src/pages/admin/DashboardPage.tsx,
    src/components/admin/OrderDetailDrawer.tsx,
    src/components/admin/ProductFormModal.tsx,
    src/components/admin/StockAdjustModal.tsx,
    src/pages/admin/LoginPage.tsx
  </files>
  <read_first>
    src/pages/admin/ProductsPage.tsx,
    src/pages/admin/OrdersPage.tsx,
    src/components/admin/OrderDetailDrawer.tsx,
    src/pages/admin/LoginPage.tsx,
    src/components/admin/ProductFormModal.tsx,
    src/components/admin/StockAdjustModal.tsx
  </read_first>
  <action>
  **Error state fixes (UX-02):**

  **ProductsPage.tsx (GAP — no error handling):**
  - Destructure `isError, error` from `useAdminProducts`
  - Add error state before the table: `if (isError) { return <Alert type="error" message="Khong tai duoc danh sach san pham" description={...} showIcon /> }`

  **OrdersPage.tsx (GAP — no error handling):**
  - Destructure `isError, error` from `useAdminOrders`
  - Add error state: `if (isError) { return <Alert type="error" message="Khong tai duoc danh sach don hang" ... /> }`

  **OrderDetailDrawer.tsx (check and fix):**
  - Read the file — verify it handles isLoading and isError from useAdminOrder
  - If missing loading state: add Spin inside drawer body
  - If missing error state: add Alert inside drawer body
  - Ensure drawer shows proper feedback, not just blank content

  **LoginPage.tsx (UX-03 — form validation):**
  - Verify AntD Form rules exist for email (required + email format) and password (required + min length)
  - If missing, add: `rules={[{ required: true, message: 'Vui long nhap email' }, { type: 'email', message: 'Email khong hop le' }]}`
  - Verify login error (401) shows visible message to user (not just silent)

  **ProductFormModal.tsx (UX-03 — already good):**
  - Has rules for name (required, min 3), price (required, min 1), unit_type (required), category (required)
  - No changes needed

  **StockAdjustModal.tsx (UX-03 — already good):**
  - Has rules for delta (required, type number), adjustment_type (required)
  - No changes needed

  **DashboardPage.tsx:**
  - Already has both loading and error states — no changes needed for UX-01/UX-02
  - (Revenue card changes in Plan 01 Task 2 — no conflict since different row)

  **Global interceptor check (axiosInstance.ts):**
  - Already handles 401 (redirect + toast) and 5xx (toast) — verify 403 is also handled
  - If 403 not handled: add `if (status === 403) { message.error('Ban khong co quyen thuc hien thao tac nay.'); }`
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
  - ProductsPage shows error Alert when API fails
  - OrdersPage shows error Alert when API fails
  - OrderDetailDrawer has both loading and error states
  - LoginPage has proper form validation rules with inline error messages
  - 403 errors produce visible notification
  - All admin TanStack Query error states surfaced to user
  </acceptance_criteria>
  <done>Every admin page has visible loading + error states; all forms have inline validation; no silent API failures anywhere in the app</done>
</task>

</tasks>

<verification>
1. Disconnect network / stop backend — verify every page shows error state (not blank)
2. Submit each form with empty required fields — verify inline errors appear
3. Check browser console — no unhandled promise rejections
4. `npx vitest run` — all tests pass
</verification>

<success_criteria>
- UX-01: Every useQuery/useMutation shows loading indicator
- UX-02: Every API error produces visible feedback (toast, Alert, or Result)
- UX-03: Every form validates before submission with specific error messages
- No regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04-account-polish/04-02-SUMMARY.md`
</output>
