---
phase: 04-account-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/pages/customer/HomePage.tsx
  - src/pages/customer/ProductDetailPage.tsx
  - src/pages/customer/CartPage.tsx
  - src/pages/customer/CheckoutPage.tsx
  - src/pages/customer/MyOrdersPage.tsx
  - src/layouts/CustomerLayout.tsx
  - src/layouts/AdminLayout.tsx
  - src/pages/admin/DashboardPage.tsx
  - src/pages/admin/ProductsPage.tsx
  - src/pages/admin/OrdersPage.tsx
  - src/index.css
autonomous: false
requirements:
  - UX-04
must_haves:
  truths:
    - "All customer pages are usable on 360px viewport without horizontal scroll"
    - "No text is truncated or actions hidden on mobile"
    - "Admin pages are functionally usable on mobile (tables scroll horizontally, forms work)"
    - "Navigation header works on mobile without overflow"
  artifacts:
    - path: "src/index.css"
      provides: "Global responsive overrides if needed"
    - path: "src/layouts/CustomerLayout.tsx"
      provides: "Mobile-friendly header navigation"
  key_links:
    - from: "All customer pages"
      to: "AntD responsive grid"
      via: "xs/sm/md/lg Col breakpoints"
      pattern: "xs={24}"
---

<objective>
Mobile responsiveness audit and fixes for 360px viewport, plus final human verification of all Phase 4 work.

Purpose: UX-04 requires the app to be fully usable on 360px mobile viewport. All customer pages must be checked for horizontal overflow, truncated content, and touch target sizing. Admin pages should be functionally usable (tables can scroll horizontally).

Output: All pages pass 360px viewport check; human verifies complete Phase 4 delivery.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-account-polish/04-01-SUMMARY.md
@.planning/phases/04-account-polish/04-02-SUMMARY.md

<interfaces>
<!-- Responsive patterns already in use -->

AntD Grid breakpoints used across pages:
- HomePage: Row/Col with xs={24} sm={8} md={6} for filters
- ProductDetailPage: Row/Col with xs={24} md={10}/md={14} for image/info split
- CartPage: Row/Col with xs={24} md={16}/md={8} for items/summary
- CheckoutPage: Row/Col with xs={24} md={14}/md={10} for form/summary
- DashboardPage: Col with xs={12} sm={8} lg={4} for stat cards, xs={24} sm={12} lg={8} for summary cards
- OrdersPage: Table with scroll={{ x: 900 }}
- ProductsPage: No explicit scroll={{ x }} (POTENTIAL GAP)

CustomerLayout header: flexbox with sticky positioning, 64px height
AdminLayout: Need to check sidebar behavior on mobile
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mobile responsiveness audit and fixes for all pages</name>
  <files>
    src/pages/customer/HomePage.tsx,
    src/pages/customer/ProductDetailPage.tsx,
    src/pages/customer/CartPage.tsx,
    src/pages/customer/CheckoutPage.tsx,
    src/pages/customer/MyOrdersPage.tsx,
    src/layouts/CustomerLayout.tsx,
    src/layouts/AdminLayout.tsx,
    src/pages/admin/DashboardPage.tsx,
    src/pages/admin/ProductsPage.tsx,
    src/pages/admin/OrdersPage.tsx,
    src/components/customer/CartItemRow.tsx,
    src/components/customer/ProductCard.tsx,
    src/index.css
  </files>
  <read_first>
    src/layouts/CustomerLayout.tsx,
    src/layouts/AdminLayout.tsx,
    src/pages/customer/HomePage.tsx,
    src/pages/customer/ProductDetailPage.tsx,
    src/pages/customer/CartPage.tsx,
    src/pages/customer/CheckoutPage.tsx,
    src/components/customer/CartItemRow.tsx,
    src/components/customer/ProductCard.tsx,
    src/pages/admin/ProductsPage.tsx,
    src/pages/admin/DashboardPage.tsx,
    src/index.css
  </read_first>
  <action>
  **Systematic 360px viewport audit. For each page, check and fix:**

  **CustomerLayout.tsx (header):**
  - Logo text + cart icon + new "Don hang" link must fit in 360px
  - If header overflows: make logo shorter on mobile, use only icons (no text) for nav items on xs screens
  - Consider: hide Menu component on mobile (it has no items currently), ensure cart icon and orders link are always visible
  - Reduce paddingInline to 16px on mobile if needed: use a responsive style or media query

  **HomePage.tsx:**
  - Filter row: xs={24} stacks vertically (GOOD)
  - ProductGrid: check ProductCard width at 360px — should show 1 card per row on xs
  - Verify Search input does not overflow

  **ProductDetailPage.tsx:**
  - Image/info split: xs={24} stacks (GOOD)
  - Check: breadcrumb wrapping, price text size, quantity input + button layout
  - Ensure "Them vao gio hang" button is full width on mobile

  **CartPage.tsx:**
  - Items/summary: xs={24} stacks (GOOD)
  - CartItemRow: check if image + name + quantity controls + price fit on 360px
  - If CartItemRow is too wide: stack elements vertically on mobile, or reduce image size
  - "Tien hanh dat hang" button should be full width

  **CheckoutPage.tsx:**
  - Form/summary: xs={24} stacks (GOOD)
  - Form inputs: full width by default (GOOD)
  - Check padding: `padding: '24px 16px'` — adequate for 360px

  **MyOrdersPage.tsx (new from Plan 01):**
  - Ensure order list/table uses responsive layout
  - If using Table: add `scroll={{ x: 600 }}` for horizontal scroll on mobile
  - If using List: verify card layout stacks properly

  **Admin pages (functional, not pixel-perfect):**

  **AdminLayout.tsx:**
  - Check if Sider has `breakpoint` and `collapsedWidth` props for mobile
  - If not: add `breakpoint="lg" collapsedWidth={0}` to make sidebar collapse on mobile
  - Ensure the layout does not cause horizontal scroll

  **ProductsPage.tsx:**
  - Table: add `scroll={{ x: 800 }}` if not present (allows horizontal scroll on mobile)
  - Action buttons Space: may overflow — consider using Dropdown menu on mobile or reducing button count

  **OrdersPage.tsx:**
  - Already has `scroll={{ x: 900 }}` (GOOD)
  - Filter Space with `wrap` (GOOD)

  **DashboardPage.tsx:**
  - Stat cards: xs={12} (2 per row on mobile) — verify they fit at 360px
  - If cards overflow: reduce to xs={24} (1 per row) or keep xs={12} with smaller padding

  **Global (src/index.css):**
  - Add `html, body { overflow-x: hidden; }` as a safety net against horizontal scroll
  - Add any needed mobile-specific overrides (e.g., AntD table responsive tweaks)
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
  - No horizontal scrollbar on any customer page at 360px viewport width
  - All text readable, all buttons tappable, no overlapping elements
  - Admin tables horizontally scrollable (not breaking layout)
  - AdminLayout sidebar collapses on mobile
  - CustomerLayout header fits within 360px without overflow
  </acceptance_criteria>
  <done>All pages pass 360px mobile viewport check with no horizontal scroll and all content accessible</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final Phase 4 human verification</name>
  <files>N/A</files>
  <action>
  Present the complete Phase 4 delivery for human verification. Run `npm run dev` to start the dev server, then guide user through verification steps covering all 7 requirements (ACCOUNT-01, ACCOUNT-02, DASH-02, UX-01, UX-02, UX-03, UX-04).
  </action>
  <what-built>
  Complete Phase 4 delivery:
  1. Customer order history page (/orders) with localStorage persistence (ACCOUNT-01, ACCOUNT-02)
  2. Admin dashboard revenue card from completed orders (DASH-02)
  3. Loading states on every data-fetching surface (UX-01)
  4. Error handling on every API call — no silent failures (UX-02)
  5. Form validation with inline errors on every form (UX-03)
  6. Mobile responsiveness at 360px viewport (UX-04)
  </what-built>
  <how-to-verify>
  **Order History (ACCOUNT-01, ACCOUNT-02):**
  1. Visit the store, add items to cart, complete checkout
  2. Navigate to /orders — verify the order appears with correct details
  3. Click/expand the order — verify items, status, address, total visible
  4. Refresh page — verify order still present
  5. Check CustomerLayout header — "Don hang" link visible

  **Dashboard Revenue (DASH-02):**
  6. Login as admin, go to /admin/dashboard
  7. Verify "Doanh thu" card shows revenue figure (or 0 if no completed orders)

  **Loading States (UX-01):**
  8. Throttle network in DevTools to Slow 3G
  9. Visit each page: /, /products/:id, /cart, /checkout, /orders, /admin/dashboard, /admin/products, /admin/orders
  10. Verify each shows a spinner/loading indicator while data loads

  **Error Handling (UX-02):**
  11. Stop the backend server
  12. Visit each page — verify error message/Alert shown (not blank screen)
  13. Submit checkout form — verify error notification appears

  **Form Validation (UX-03):**
  14. Go to /checkout, click "Dat hang ngay" with empty fields — verify inline errors
  15. Go to /admin/login, submit empty — verify inline errors
  16. Go to /admin/products, add product with empty name — verify inline error

  **Mobile (UX-04):**
  17. Open Chrome DevTools, set viewport to 360x640
  18. Navigate through all customer pages — verify no horizontal scroll
  19. Check admin pages — verify tables scroll horizontally, sidebar collapses
  </how-to-verify>
  <verify>Human confirms all 7 requirements pass visual and functional check</verify>
  <acceptance_criteria>
  - All ACCOUNT, DASH-02, and UX requirements verified working by human
  - No regressions in Phase 1-3 functionality
  </acceptance_criteria>
  <done>Human types "approved" — all Phase 4 requirements verified working end-to-end</done>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Complete Phase 4 verification:
1. All 7 requirement IDs satisfied: ACCOUNT-01, ACCOUNT-02, DASH-02, UX-01, UX-02, UX-03, UX-04
2. No regressions in Phase 1-3 functionality
3. Human verification confirms all success criteria
</verification>

<success_criteria>
- UX-04: App fully usable on 360px viewport, no horizontal scroll on customer pages
- Human approves all Phase 4 deliverables
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-account-polish/04-03-SUMMARY.md`
</output>
